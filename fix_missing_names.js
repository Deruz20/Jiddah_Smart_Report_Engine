const { createClient } = require("@supabase/supabase-js"); const supabase = createClient("https://vismrobdsdsaxmqegcay.supabase.co", process.env.SUPABASE_SERVICE_ROLE_KEY); const names = { "muhsin": "????", "leilah": "????", "malik": "????", "sumayyah": "????", "hakiimah": "?????", "musa": "????", "amiirat": "?????", "shuraim": "????", "sameeha": "?????", "imran": "?????", "shukran": "?????", "asma": "?????", "maysarat": "?????", "faham": "???", "arham": "????", "rahmah": "????", "yahya": "????" }; async function run() { const { data: students, error } = await supabase.from("students").select("id, name"); if (error) { console.error("Error fetching students:", error); return; } let updatedCount = 0; for (const s of students) { const lowered = s.name.toLowerCase(); for (const [eng, ar] of Object.entries(names)) { if (lowered.includes(eng)) { console.log(`Updating ${s.name} to have Arabic name containing ${ar}`); // Only updating if it doesnt already match exactly to avoid overriding perfectly phonetically matched surnames unless we construct the whole name? No, wait. We can just run the full transliterator on them. } } } } run();
