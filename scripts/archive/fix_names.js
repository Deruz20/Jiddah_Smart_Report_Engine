require("dotenv").config({ path: ".env.local" }); const { createClient } = require("@supabase/supabase-js"); async function run() { const url = process.env.NEXT_PUBLIC_SUPABASE_URL; const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; const supabase = createClient(url, key); const { data, error } = await supabase.from("students").update({ arabic_name: null }).not("name", "is", null); if (error) return console.error("Error:", error); console.log("Successfully cleared arabic_name for all students."); } run();
