const { createClient } = require("@supabase/supabase-js"); const supabase = createClient("https://vismrobdsdsaxmqegcay.supabase.co", process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data, error } = await supabase.from("enrollments").select(`students (name), theology_classes (class_name_english), theology_class_id`).eq("is_active", true); if (error) console.error("Error:", error); else { console.log("Total active enrollments:", data.length); console.log("Enrollments with theology class:", data.filter(d => d.theology_class_id).length); console.log(JSON.stringify(data.filter(d => d.theology_class_id && d.theology_classes.class_name_english.includes("2")), null, 2)); } } run();
