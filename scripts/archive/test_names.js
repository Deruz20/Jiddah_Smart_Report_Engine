const { createClient } = require("@supabase/supabase-js"); const fs = require("fs"); async function run() { const env = fs.readFileSync(".env.local", "utf-8"); const urlMatch = env.match(/NEXT_PUBLIC_SUPABASE_URL=(.*)/); const keyMatch = env.match(/NEXT_PUBLIC_SUPABASE_ANON_KEY=(.*)/); const supabase = createClient(urlMatch[1], keyMatch[1]); const { data, error } = await supabase.from("students").select("name, arabic_name"); console.log("DB Sample:"); data.slice(0, 15).forEach(d => console.log(`${d.name} -> ${d.arabic_name}`)); const namesToTest = ["Musa", "Yahaya", "Umar", "Khadijah", "Sumayyah", "Amiirah", "Nasheem", "Nampeera", "Kangu"]; const res = await fetch("http://localhost:3000/api/transliterate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ names: namesToTest }) }); if (res.ok) { const result = await res.json(); console.log("\nAPI test:"); namesToTest.forEach((n, i) => { console.log(`${n} -> ${result.transliterated[i]}`); }); } } run();
