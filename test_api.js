async function run() { const namesToTest = ["Hassan", "Musa", "Khadijah", "Yahaya", "Kangu", "Lubwama", "Amiirah", "Twaibu"]; const res = await fetch("http://localhost:3000/api/transliterate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ names: namesToTest }) }); if (res.ok) { const result = await res.json(); console.log("\nAPI returned transliterated names:"); namesToTest.forEach((n, i) => { console.log(`${n} -> ${result.transliterated[i]}`); }); } else { console.log("API Error:", res.status, await res.text()); } } run();
